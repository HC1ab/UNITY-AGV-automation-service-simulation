# AGV Automation Service Simulation — 프로젝트 기준

## 좌표/스케일 단위 기준

**1 Unity 유닛 = 실제 1미터(m)**

AGV 이동 거리·속도 시뮬레이션이 목적이므로, 씬 내 모든 좌표와 크기는 실제 축척(미터)을 그대로 따른다. 별도의 스케일 팩터를 걸지 않는다.

## 부산항 신항 터미널 실측 데이터 (기준 자료: 부산항만공사 busanpa.com, 확인일 2026-07-09)

| 부두 | 코드 | 운영사 | 안벽길이(m) | 선석수 | 부지면적(㎡) |
|---|---|---|---|---|---|
| 1부두 | PNIT | 부산신항국제터미널㈜ | 1,200 | 3 | 840,000 |
| 2부두 | PNC | 부산신항만㈜ | 2,000 | 6 | 1,210,000 |
| 3부두 | HJNC | 한진부산컨테이너터미널㈜ | 1,100 | 4 | 688,000 |
| 4부두 | HPNT | HMM PSA신항만㈜ | 1,150 | 4 | 553,000 |
| 5부두 | BNCT | ㈜비엔씨티 | 1,400 | 4 | 785,000 |
| 6부두 | BCT | ㈜부산컨테이너터미널 | 1,050 | 3 | 631,000 |
| 7부두 | DGT | 동원글로벌터미널부산㈜ | 1,050 | 3 | 837,000 |

**합계: 안벽 8,950m / 27선석 / 부지 5,544,000㎡**

- 안벽길이(quay length) = 부두의 X축 폭(미터)으로 직접 사용
- 부두 안쪽 깊이 = `부지면적 ÷ 안벽길이`로 역산한 평균 깊이 (터미널 형상이 사각형이 아니므로 근사치)
- 선석 좌표·개수는 위 표의 선석수를 정확히 따를 것 (예: HPNT는 4선석 — MS4-01~04)

## 터미널 형상(outline) 관련 주의사항

- `Assets/Scripts/PortMapBuilder.cs`의 터미널 outline 좌표는 스크린샷을 눈대중으로 트레이싱한 **근사치**이며, 정밀 GIS 데이터가 아니다.
- 실제 안벽길이 비율과 다르게 그려진 상태(예: PNIT를 PNC보다 넓게 그림)를 위 표 기준으로 재작업해야 함.
- 인접 터미널(HJNC-PNC-PNIT 등) 경계는 반드시 좌표를 동일하게 맞춰 틈이 생기지 않게 할 것.

## Unity MCP 작업 시 참고

- 스크립트 파일을 수정한 뒤 Unity가 바로 재컴파일하지 않는 경우가 있음 → `manage_editor`/`refresh_unity`(mode=force, compile=request)로 강제 리프레시 필요.
- `PortMapBuilder.Generate()`는 항상 `terminals` 리스트를 비우고 `BuildSampleData()`로 다시 채운 뒤 생성함 (코드 수정이 즉시 반영되도록). 인스펙터에서 수동으로 좌표를 편집해도 다음 Generate 호출 시 덮어써짐.
